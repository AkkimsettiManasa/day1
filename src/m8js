function Card(props){
    const width=300;
    return(
        <div>
            <img src="/link .png" width={300}
            style={{border:'10px solid#FF000',borderRadius:'50px'}}/>
            <h1>{props.user.name}</h1>
            <h2>{props.user.branch}</h2>
            <h3>year:{props.user.year}</h3>
            <button type="button" class="btn btn-success">Success</button>


        </div>
    );
}
export default Card;